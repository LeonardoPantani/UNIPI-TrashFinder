<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="trashfinder">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_appicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrashFinder">
        <activity
            android:name="it.unipi.di.pantani.trashfinder.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TrashFinder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Settings -->
        <activity
            android:name="it.unipi.di.pantani.trashfinder.settings.SettingsActivity"
            android:label="@string/label_settings">
        </activity>

        <!-- Activity Feedback -->
        <activity
            android:name="it.unipi.di.pantani.trashfinder.feedback.FeedbackActivity"
            android:label="@string/label_feedback">
        </activity>

        <!-- Google Maps Metadata -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>